# EcoCell 使用说明

## 1. 系统用途

EcoCell 用于观察细胞类型之间的轨迹关系和细胞间通信。系统从左到右分为三个区域：

- **左栏 Trajectory Selection View**：选择感兴趣的细胞类型、起点和终点，并从具体路径中选择分支。
- **中栏 Cell Trajectory View**：查看选中路径的轨迹、每个轨迹节点的邻居通信，以及节点在整体细胞空间中的位置。
- **右栏 Gene Communication View**：查看选中节点之间具体的配体-受体通信通道。

请使用 Live Server 或其他本地 HTTP 服务打开页面。首次使用时，建议先从左栏选择路径，再在中栏进行节点和弧线操作。

## 2. 页面总体布局

```text
┌──────────────────────┬──────────────────────────────────────┬─────────────────────┐
│ Trajectory           │ Cell Trajectory View                  │ Gene Communication  │
│ Selection View       │ 轨迹分支、环形通信图、空间位置图       │ View                │
│                      │ Neighbor Overall Communication        │                     │
└──────────────────────┴──────────────────────────────────────┴─────────────────────┘
```

顶部的 **EcoCell** 是系统名称。三个区域中的颜色代表细胞类型，并在不同图表中保持一致。

## 3. 左栏：Trajectory Selection View

左栏用于确定要研究的细胞类型和轨迹范围。它包含 **Cell Distribution Summary**、**Cell Trajectory Summary** 和 **Path Pattern Tree**。

### 3.1 Cell Distribution Summary

这个列表显示数据集中各细胞类型的总体分布。每个细胞类型是一张可点击的条目；可以选择一个或多个细胞类型。选择后，下面的路径模式会只保留与这些细胞类型相关的起点或终点。

| 界面元素 | CSV 字段 | 含义 |
| --- | --- | --- |
| 彩色方块与细胞名称 | `annotation` | 细胞类型名称，例如 `Brain`、`Neural crest`；颜色与中栏和右栏保持一致 |
| 百分比 | `ratio` | 该细胞类型占全部细胞的比例，界面显示为百分比 |
| O | `out` | 与该细胞类型相关的输出方向统计 |
| I | `in` | 与该细胞类型相关的输入方向统计 |
| oT | `outtype` | 输出方向涉及的邻居细胞类型数量或类型统计 |
| iT | `intype` | 输入方向涉及的邻居细胞类型数量或类型统计 |
| 数据集总数（统计字段，不单独显示） | `total` | 该细胞类型在数据集中的细胞总数 |
| 时间点数量（统计字段，不单独显示） | `time1`、`time2` | 该细胞类型在两个统计时间点的细胞数量 |

卡片上直接看到的是细胞名称、百分比和 O/I/oT/iT；`total`、`time1`、`time2` 是同一份统计表中的辅助字段。O/I 适合用来快速比较输出和输入相关程度；oT/iT 更接近“涉及多少类细胞”的方向性概览。它们是路径分析中的统计摘要，不等同于某一条具体通信通道的强度。

### 3.2 Cell Trajectory Summary

上方列表的每一行是一个“细胞类型起点 → 细胞类型终点”的轨迹模式。列表中的灰色条用于比较不同模式的长度和数量。

| 界面元素 | CSV 字段 | 含义 |
| --- | --- | --- |
| Start Node | `start` | 轨迹的起始细胞类型 |
| End Node | `end` | 轨迹的结束细胞类型 |
| Path Length | `path_length` | 原始起点-终点模式的路径长度统计；数值越大，表示中间经过的轨迹层级通常越多 |
| Path Count | `path_num` | 原始数据中满足该起点和终点条件的路径数量 |

点击某一行后，系统会在该行下方展开与该模式匹配的具体路径树。列表中的长度条和数量条会优先按具体路径去重后的结果计算；只有无法读取具体路径时才回退到 `path_length` 和 `path_num`。路径详情还包含以下信息。这些字段用于构建树和后续统计，不会全部作为独立控件显示：

| 路径信息 | 字段 | 含义 |
| --- | --- | --- |
| 路径编号 | `path_id` | 每条具体路径的唯一编号 |
| 路径长度 | `length` | 该具体路径包含的轨迹层级数 |
| 路径权重 | `weight` | 该路径在原始路径统计中的权重或支持度 |
| 起始具体细胞 | `start_node` | 带有具体编号的起始细胞，例如 `Neural crest_1_17` |
| 结束具体细胞 | `end_node` | 带有具体编号的结束细胞 |
| 完整路径 | `path_string` | 用箭头连接的完整细胞路径 |

### 3.3 Path Pattern Tree

Path Pattern Tree 是具体路径的选择器。它显示所选起点和终点之间可能经过的细胞类型分支。

- 每个圆点代表一个细胞类型节点。
- 节点颜色代表该细胞类型，并与中栏环图和右栏图表保持一致。
- 节点下方文字是细胞类型名称。
- 节点上的 `+` 表示还有未展开的后代，`−` 表示当前已经展开。
- 树深度较大的节点默认折叠，便于先观察整体结构。
- 单击节点：选择或取消选择该分支。
- 普通单击会清除之前的单选；按住 `Ctrl`、`Shift` 或 `Meta` 可以多选。
- 双击有后代的节点：展开或折叠，不作为普通选择操作。
- 鼠标滚轮可以缩放树，拖拽可以移动树的位置。

选择多个节点时，系统使用这些节点对应路径的**并集**：重复路径只保留一次。之后，中栏会根据所选路径显示轨迹分支；选择到较深层级时，中栏也会相应显示到更深的轨迹位置。这里的选择只决定中栏可展示的分支数据，不会把这些路径直接标记为中栏的高亮路径；中栏路径高亮需要在 Cell Trajectory View 内操作连接线。

## 4. 中栏：Cell Trajectory View

中栏是系统的主要观察区域。它由顶部控制条、轨迹分支、节点环图、中心细胞位置图和下方的 Neighbor Overall Communication 组成。

### 4.1 全局控制条

控制条位于中栏轨迹图上方。

| 控件 | 可选值/操作 | 作用 |
| --- | --- | --- |
| Mouse Mode | `Pan`、`Lasso` | `Pan` 用于移动和缩放轨迹；`Lasso` 用于圈选节点 |
| Metric | `Total Intensity`、`Channel Count`、`Avg Channel Count`、`Avg Channel Intensity`、`Cell Avg Intensity` | 决定环图中发送弧和接收弧的厚度所代表的指标 |
| Clear | 点击按钮 | 清除当前轨迹、预览、套索选择和下方的邻域总体通信结果 |

键盘快捷键：当鼠标没有停留在输入控件上时，按 `P` 切换到 Pan，按 `L` 切换到 Lasso。

### 4.2 轨迹分支布局

中栏按照轨迹深度从左到右排列：

- 最左侧节点是路径起点。
- 向右每一列表示轨迹继续向后发展一个层级。
- 同一列出现多个节点时，表示路径在该层级发生了分支。
- 节点之间的箭头表示轨迹方向，从左向右指向下一层节点。
- 同一种细胞类型如果位于不同轨迹位置，仍然是不同的路径节点。

### 4.3 节点环图的视觉编码

每一个轨迹节点都显示为一个环形通信图。它表示“当前路径节点”与其他邻居细胞类型之间的通信概况。

| 图形元素 | 含义 |
| --- | --- |
| 环内标题 | 当前轨迹节点的细胞类型名称；文字沿圆弧排列，名称较长时会随圆圈弯曲 |
| 中心灰色散点 | 整体细胞空间中的位置背景，用来显示该节点处于整体细胞分布的什么位置 |
| 中心彩色散点 | 当前选中的目标细胞类型在空间中的位置 |
| 中心圆边界 | 中心空间位置图的显示范围 |
| 内弧 | 当前细胞从邻居细胞接收的通信 |
| 外弧 | 当前细胞向邻居细胞发送的通信 |
| 弧段颜色 | 邻居细胞类型的颜色；例如绿色通常代表 `Neural crest` |
| 弧段角度 | 邻居细胞在当前通信槽位中的数量或容量比例 |
| 弧段厚度 | 当前选择的 Metric 对应的通信大小 |
| 环上的空白 | 当前节点没有占满该邻居类型的可用槽位，或当前没有对应通信 |
| 悬停提示 | 显示邻居细胞类型、细胞数量以及发送或接收强度 |
| 选中路径的高亮圆环 | 表示当前正在预览或锁定的路径；当前使用蓝色渐变高亮，只包住环图，不覆盖标题 |

内弧和外弧的方向含义如下：

```text
邻居细胞  ──发送──>  当前轨迹节点   = 当前节点的内弧（接收）
当前轨迹节点 ──发送──> 邻居细胞     = 当前节点的外弧（发送）
```

### 4.4 通信指标的字段映射

| Metric 控件 | 发送值 | 接收值 | 适合回答的问题 |
| --- | --- | --- | --- |
| Total Intensity | `发送总强度` | `接收总强度` | 当前节点整体发送或接收通信有多强 |
| Channel Count | `发送通道数` | `接收通道数` | 当前节点使用了多少条通信通道 |
| Avg Channel Count | `平均发送通道数` | `平均接收通道数` | 平均到单个细胞后，每个细胞涉及多少通道 |
| Avg Channel Intensity | `发送平均强度` | `接收平均强度` | 单条通道的平均通信强度 |
| Cell Avg Intensity | `细胞发送平均强度` | `细胞接收平均强度` | 平均到单个细胞后的发送或接收强度 |

切换 Metric 后，所有已经显示的环图和下方的总体通信图会使用同一个指标重新绘制。弧段厚度主要用于比较大小，不建议把不同 Metric 下的厚度直接当作同一种量比较。

### 4.5 中心细胞位置图

每个环图中心的小散点图表示细胞在整体 embedding 空间中的相对位置。它帮助使用者判断：某个轨迹节点对应的细胞是否聚集在某个空间区域，以及目标细胞与其他细胞的空间关系。

| 图形或数据元素 | 含义 |
| --- | --- |
| 灰色点 | 整体 embedding 空间中的其他细胞 |
| 彩色点 | 当前轨迹节点对应的目标细胞 |
| 点的位置 | 细胞在 embedding 平面上的相对位置；距离只用于空间关系观察，不直接等同于发育时间或通信强度 |
| `annotation` | 细胞类型注释 |
| `x`、`y` | embedding 平面坐标 |
| `time` | 细胞所属的时间信息 |
| `embedding_level` | embedding 数据的层级 |
| `embedding_index` | embedding 中的具体索引 |
| 目标细胞标识 | 由细胞注释、时间和 embedding 信息组合，用于匹配具体路径细胞 |

当鼠标悬停在环图弧段上时，中心位置图可以进一步高亮该邻居细胞类型在空间中的分布。

### 4.6 轨迹连接线和路径预览

相邻列的节点通过由水平段、垂直段和圆角转折组成的连接线连接，末端带方向箭头。连接线表示轨迹从一个节点发展到下一个节点的方向。

- **悬停连接线**：临时高亮从起点到当前连接的完整前级路径，包括节点、连接线和方向箭头，并在路径节点上显示对应的预览环图。
- **点击连接线**：锁定从起点到当前连接的完整路径预览；不属于该路径的其他分支保持普通灰色。
- **再次点击同一条线**：解除锁定，恢复普通显示。
- **点击另一条线**：切换到另一条路径的预览。
- 预览状态只突出环图区域（预览环图容器为 `.path-preview-ring`），不覆盖节点名称。
- 左栏 Cell Trajectory Summary 和 Path Pattern Tree 负责提供可展示的轨迹分支，不会直接触发这里的蓝色路径高亮。

### 4.7 Pan、缩放和 Clear

- **Pan 模式**：按住鼠标左键拖拽，移动整张轨迹图。
- **滚轮缩放**：缩放轨迹图，范围约为 `0.3` 到 `3` 倍。
- **Shift + 滚轮**：水平移动轨迹图。
- **双击轨迹空白处**：恢复默认缩放和位置。
- **Lasso 模式**：按住鼠标左键绘制一个圈选区域。
- **Clear**：清除轨迹节点、路径预览、锁定状态、套索状态和下方的 Neighbor Overall Communication，并恢复默认指标。

## 5. Lasso 选择与 Neighbor Overall Communication

### 5.1 如何使用 Lasso 选择节点

1. 在中栏控制条中把 Mouse Mode 切换为 `Lasso`。
2. 在轨迹图上用鼠标圈住一个或多个环形节点。
3. 松开鼠标后，系统会判断哪些节点的中心位于圈选区域内。
4. 系统会读取每个被选节点对应的具体细胞集合，并收集这些细胞的邻居类型。

这里选择的是**轨迹节点**，不是单个灰色或彩色散点。一个轨迹节点可能对应许多具体细胞，例如一个 `Neural crest` 节点可能对应 `Neural crest_1_17`、`Neural crest_1_15` 等多个具体细胞。

### 5.2 Lasso 选择后中栏会发生什么

套索选择完成后，中栏下方会新增一个 Neighbor Overall Communication 分析条目。该条目使用本次套索选中的节点作为一条有顺序的路径：

- 被圈中的节点数量决定 `Nodes` 的数值。
- 节点在轨迹中的先后顺序决定 `Path` 的显示顺序。
- 每个节点对应的具体细胞会被合并计算，而不是只取一个代表细胞。
- 所有被选节点的邻居类型会汇总到 `Neighbors` 中。
- 每次新的套索分析会新增一个条目，多个条目可以在下方滚动查看。
- 点击某个条目的删除按钮，只删除这一条分析，不会删除轨迹图。

### 5.3 Neighbor Overall Communication 条目

每个条目包括：

- **标题**：例如 `Lasso Selection (4 nodes)`，表示本次圈选包含 4 个轨迹节点。
- **Path**：按路径顺序显示彩色细胞类型圆点、名称和箭头。
- **Nodes**：本次分析包含的轨迹节点数量。
- **Neighbors**：这些节点汇总得到的邻居细胞类型数量。
- **删除按钮**：只移除当前分析条目。

每个条目内部横向排列三个图：

1. **Population Dynamics**：显示每个路径位置的细胞总数变化。它回答“路径上不同位置包含多少细胞”。
2. **Incoming Signals**：按邻居细胞类型堆叠的接收通信柱状图。它回答“每个路径位置从哪些邻居接收了多少通信”。
3. **Outgoing Signals**：按邻居细胞类型堆叠的发送通信柱状图。它回答“每个路径位置向哪些邻居发送了多少通信”。

如果切换中栏的 Metric，这三个图也会同步切换指标。总体图用于比较节点之间的整体差异，不显示单条配体-受体通道的细节。

## 6. 右栏：Gene Communication View

右栏用于查看具体的基因通信通道。要进行多节点通道分析，必须先在中栏使用 Lasso 至少选中两个轨迹节点，再点击中栏环图的内弧或外弧。只圈选一个节点时，仍可在中栏查看环图通信概况，但不会进入右栏的多节点套索热图状态。

### 6.1 Control Panel

右栏顶部是控制区：

| 控件 | 可选值/操作 | 作用 |
| --- | --- | --- |
| Gene | `all` 或具体基因 | 只显示包含该配体或受体基因的通信通道 |
| Rank | `default` 或某个节点 | 按指定节点上的通道强度重新排序 |
| Count | `5`、`10`、`20`、`50`、`100`、`200` | 控制热图最多显示多少条通道，默认显示 20 条 |
| Clean | 点击按钮 | 清空热图并恢复默认筛选条件 |

初始状态会显示提示文字。完成 Lasso 选择前，右栏不会显示本次多节点分析的通道热图。

### 6.2 从环图弧线进入通道分析

- 点击**内弧**：查看当前节点从指定邻居接收的通信通道。
- 点击**外弧**：查看当前节点向指定邻居发送的通信通道。
- Lasso 分析中，系统显示套索选中的多个路径节点在该邻居方向上的通道，并把每个路径节点作为一列。
- 如果只想查看中栏的环图通信概况，可以直接观察弧段和悬停提示；右栏的多节点通道热图仍然按照“先 Lasso、后点击弧线”的步骤使用。

### 6.3 通道数据字段

右栏使用的通道数据包含以下字段：

| 界面或数据元素 | 字段 | 含义 |
| --- | --- | --- |
| 邻居细胞 | `邻居细胞` | 与当前细胞发生通信的邻居细胞类型 |
| 方向 | `方向` | `发送` 表示当前细胞向邻居发送；`接收` 表示当前细胞从邻居接收 |
| 排名 | `排名` | 原始通道排名 |
| 通道 | `通道` | 配体-受体通道名称，例如 `IGF2_IGF1R` |
| 目标细胞 | `目标细胞` | 当前分析中的目标细胞类型 |
| 目标细胞基因 | `目标细胞基因` | 目标细胞一侧的配体或受体基因 |
| 邻居细胞基因 | `邻居细胞基因` | 邻居细胞一侧的配体或受体基因 |
| 强度 | `强度` | 该通道的原始通信强度 |
| 显著性 | `显著性` | 该通道的统计显著性指标 |

当同一通道在多个具体细胞中出现时，系统会把强度相加、把显著性取平均，再按具体细胞总数进行归一化。这样得到的是路径节点层面的平均通道强度，而不是某一个单细胞的原始值。

### 6.4 热图和小提琴图

热图的每一列代表一个路径节点，每一行代表一个通信通道。单元格颜色越深，表示该通道在该节点上的归一化强度越高；浅色单元格表示强度低或没有观测到该通道。

每一行热图左侧的深灰色标签显示目标细胞一侧的基因，右侧的中灰色标签显示邻居细胞一侧的基因。左侧标签的右边缘与热图起点对齐，避免被第一列热图单元格覆盖。

热图上方的 **Channel Intensity Distribution** 小提琴图显示每个节点的通道强度分布：

- **小提琴宽度**：该强度附近的数据密度。
- **中间横线**：强度中位数。
- **白色箱体**：中间 50% 数据所在的四分位区间。
- **纵轴**：`Intensity (×10⁻⁴)`，表示归一化后的强度尺度。

当路径中重复出现同一种细胞类型时，显示名会增加位置编号，例如 `Brain(1)`、`Brain(2)`，这样可以区分它们在路径中的不同位置。

### 6.5 套索后的通道热图

Lasso 选择两个或以上路径节点后，右栏会进入多节点通道分析状态。此时必须进一步点击中栏某个节点的环图弧线：

- 点击内弧：查看这些路径节点从所选邻居接收的通道。
- 点击外弧：查看这些路径节点向所选邻居发送的通道。
- 热图的列是套索选中的路径节点。
- 热图的行是该邻居方向上的配体-受体通道。
- 同名细胞类型如果位于路径的不同位置，会分别显示，避免不同节点的数据混在一起。

右栏的热图回答的是“具体是哪一条配体-受体通道在起作用”；中栏下方的总体通信图回答的是“整体通信量和邻居组成如何变化”。两者互相补充，不能相互替代。

## 7. 从用户操作到结果

### 普通路径流程

```text
选择左栏细胞类型
  -> 过滤 Cell Trajectory Summary
  -> 点击起点-终点模式
  -> 展开 Path Pattern Tree
  -> 选择一个或多个路径节点
  -> 中栏显示轨迹分支和环形通信图
  -> 环图中心显示目标细胞的空间位置
```

### Lasso 与总体通信流程

```text
Mouse Mode = Lasso
  -> 圈选一个或多个中栏轨迹节点
  -> 系统读取这些节点对应的具体细胞
  -> 汇总邻居细胞类型
  -> 中栏下方新增 Neighbor Overall Communication
  -> 查看细胞总数、接收通信和发送通信
```

### Lasso 与具体通道流程

```text
完成 Lasso 选择
  -> 点击中栏某节点的内弧或外弧
  -> 选择接收或发送方向
  -> 读取对应邻居方向的配体-受体数据
  -> 右栏显示小提琴图和通道热图
  -> 使用 Gene、Rank、Count 进一步筛选和排序
```

## 8. 颜色和方向的阅读规则

颜色在不同视图中代表同一个细胞类型。例如：

| 细胞类型 | 颜色 |
| --- | --- |
| Heart | 粉红色 |
| Neural crest | 绿色 |
| Branchial arch | 棕色 |
| AGM | 紫色 |
| Liver | 金黄色 |
| Cavity | 深橙色 |
| Brain | 浅米色 |
| Connective tissue | 青绿色 |
| Dermomyotome | 粉紫色 |
| Mesenchyme | 深红色 |
| Notochord | 橙色 |
| Sclerotome | 深灰色 |

阅读通信方向时请记住：

- 当前节点的**内弧**表示它在接收通信。
- 当前节点的**外弧**表示它在发送通信。
- 环图弧段颜色表示邻居细胞类型，不表示发送或接收方向。
- 弧段厚度表示当前 Metric 下的大小，不同 Metric 的厚度不能直接当成同一种数值比较。
- `IGF2_IGF1R` 这类名称表示一条配体-受体通信通道，具体哪一侧是配体或受体要结合目标细胞基因和邻居细胞基因字段判断。
